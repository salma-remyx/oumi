# Job config to LoRA tune Gemma 4 26B (Mixture-of-Experts).
#
# Requirements:
#   - Set up SkyPilot GCP: https://oumi.ai/docs/en/latest/user_guides/launch/launch.html#setup
#   - Gemma license acceptance required: https://huggingface.co/google/gemma-4-26B-A4B-it
#   - Log into WandB (`wandb login`) or disable `enable_wandb`
#
# Usage:
#   oumi launch up -c oumi://configs/recipes/gemma4/sft/26b_lora/gcp_job.yaml --cluster gemma4-26b-lora
#
# See Also:
#   - Documentation: https://oumi.ai/docs/en/latest/user_guides/launch/launch.html
#   - Config class: oumi.core.configs.JobConfig
#   - Config source: https://github.com/oumi-ai/oumi/blob/main/src/oumi/core/configs/job_config.py
#   - Other job configs: configs/**/*job.yaml

name: gemma4-26b-lora

resources:
  cloud: gcp
  accelerators: "A100:8"
  use_spot: false
  disk_size: 500  # Disk size in GBs

working_dir: .

file_mounts:
  ~/.netrc: ~/.netrc  # WandB credentials

envs:
  WANDB_PROJECT: oumi-train
  OUMI_RUN_NAME: gemma4-26b.lora

setup: |
  set -e
  pip install uv && uv pip install --system oumi[gpu] hf_transfer

run: |
  set -e  # Exit if any command failed.
  source ./configs/examples/misc/sky_init.sh

  set -x
  oumi distributed torchrun -m oumi train \
      -c oumi://configs/recipes/gemma4/sft/26b_lora/train.yaml \
      --training.run_name "${OUMI_RUN_NAME}.${SKYPILOT_TASK_ID}"

  echo "Node ${SKYPILOT_NODE_RANK} is all done!"
